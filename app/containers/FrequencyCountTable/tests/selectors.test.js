// import { fromJS } from 'immutable';
// import { selectFrequencyCountTableDomain } from '../selectors';

describe('selectFrequencyCountTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
