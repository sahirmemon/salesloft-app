/*
 * LoadingMessage Messages
 *
 * This contains all the text for the LoadingMessage component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  loading: {
    id: 'app.components.LoadingMessage.loading',
    defaultMessage: 'Looks like we are still loading...',
  },
});
